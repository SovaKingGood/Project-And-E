{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "ESP-IDF Build",
            "type": "shell",
            "command": "idf.py build",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}

